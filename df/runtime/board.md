# The Factory Runtime Board

This is the live task queue. Agents must update it when task state changes.

| Priority | Task ID | Title | Type | State | Owner role | Blocked? | Last updated | Next action |
|---|---|---|---|---|---|---|---|---|
| P0 | TUT-LINK-INTEGRITY | Plan complete seeded-site link integrity remediation | Bug | DONE | factory | No | 2026-07-11 17:24 CEST | Child delivery tasks implement contracts, seed graph, rendering, and full-site link verification |
| P0 | TUT-LINK-CONTRACTS | Correct TUT-USA component link contracts | Bug | DONE | backend-dev | No | 2026-07-11 21:21 CEST | Completed: V18 contracts, migration/public-registry tests, PostgreSQL execution, all Maven gates, and Docker image/runtime validation pass |
| P0 | TUT-LINK-SEED | Rebuild and validate the TUT-USA seeded link graph | Bug | DONE | data-engineer | No | 2026-07-12 10:22 CEST | Completed: explicit V18 link graph, pre-write validation, 17/17 script tests, two live reseeds, persisted quality checks, full builds, and Selenium gates pass |
| P0 | TUT-LINK-RENDERING | Render and verify every seeded TUT-USA link | Bug | DONE | frontend-dev | No | 2026-07-12 local | Completed: authored-link rendering, unit coverage, 22/22 template Selenium checks, smoke/full gates, and retained artifacts all pass |
| P1 | BUG-TUT-VEHICLE-RENDERER | Replace generic vehicle, campaign, and learning contract renderers | Bug | DONE | factory | No | 2026-07-12 local | Completed: concrete vehicle/campaign/learning renderers, image-object normalization, 27 unit tests, frontend builds, and Selenium smoke/full gates pass |
| P1 | RUNTIME-ES-HEALTH | Remediate Elasticsearch aggregate actuator health compatibility | Bug | DONE | factory | No | 2026-07-11 12:49 local | Completed: Elasticsearch health compatibility fix, full backend tests/compile, and live actuator verification all passed |
| P1 | CONTENT-PUBLISH-DOUBLECLICK | Open content-tree pages in Publish on double-click | Bug | DONE | frontend-dev | No | 2026-08-25 local | Chromium installed and the suite run for the first time: admin-e2e **147/147**. `UI-016b` was failing on a fixture that typed the locale node as `flexcms/page`, which the tree never descends into |
| P1 | SITE-PAGE-REFERENCE-RENDERING | Keep nested page references out of public page content | Bug | DONE | frontend-dev | No | 2026-07-11 local | Renderer boundary, asset normalization, unit/Selenium coverage, and live Vehicles HTML verification passed |
| P0 | BUG-CONTENT-DELETE | Content node deletion fails for every node | Bug | DONE | backend-dev | No | 2026-08-19 23:05 CEDT | Completed: `@Modifying` + sibling-safe prefix, 5 new tests, verified live (delete cascades, shared-prefix siblings preserved); REB-19 fixtures now clean up |
| P0 | BUG-PUBLISH-REPLICATION | Publishing a node does not replicate it to the publish environment | Bug | DONE | backend-dev | No | 2026-08-19 23:05 CEDT | Completed: `ContentStatusChangedEvent` + AFTER_COMMIT replication listener, controller loop removed, 9 new tests, verified live via `/node/status` alone |
| P1 | INFRA-TESTCONTAINERS-DOCKER29 | Integration tests cannot run on Docker Engine 29+ and are not wired into any build stage | Bug | DONE | devops | No | 2026-08-22 23:55 CEDT | Completed: Testcontainers 2.0.5 inherited from the Boot parent, failsafe bound to `verify`; all 4 ITs pass on Engine 29.7.2 (47 IT + 505 unit = 552 tests, 0 failures); gate proven by deliberate breakage; CLAUDE.md updated; new blocker I29-1 (PIM seed status enum) needs SA routing |
| P0 | DFCA-01 | Replace local Copilot CLI with Copilot Cloud Agent REST orchestration | Task | BLOCKED | qa | No | 2026-07-07 local | Cloud agent blocked: cloud=timed_out ci=not_checked |
| P0 | REB-00 | Rebuild backlog reset and delivery architecture | Task | DONE | po | No | 2026-07-07 local | Accepted by PO; designer starts REB-01 next |
| P0 | REB-01 | Normalize TUT design packages and approve storage map | Task | READY_FOR_PO | po | No | 2026-07-07 local | PO reviews QA report and accepts/rejects `REB-01` |
| P0 | REB-02 | Build Selenium browser asset-capture pipeline for remote template resources | Task | DONE | po | No | 2026-07-08 local | Verified by USER; accepted complete |
| P0 | REB-03 | Reset existing TUT/demo seed data safely and create idempotent reseed plan | Task | DONE | po | No | 2026-07-08 local | Verified by USER; accepted complete |
| P0 | REB-04 | Generate component/template/page-tree contracts from inventory | Task | DONE | po | No | 2026-07-08 local | Verified by USER; accepted complete |
| P0 | REB-05 | Add Selenium framework foundation and reporting package | Task | DONE | po | No | 2026-07-08 local | Verified by USER; accepted complete |
| P1 | REB-06 | Produce Selenium traceability matrix and generated test-case skeletons | Task | DONE | po | No | 2026-07-08 local | Verified by USER; accepted complete |
| P1 | REB-07 | Import captured assets into DAM/public frontend asset pipeline | Task | DONE | po | No | 2026-07-08 local | Verified by USER; accepted complete |
| P1 | REB-08 | Rebuild frontend tokens, fonts, layout shell, and renderer foundation | Task | DONE | po | No | 2026-07-08 local | Verified by USER; accepted complete |
| P1 | REB-09 | Implement TUT grouped component renderers | Task | DONE | frontend-dev | No | 2026-07-08 local | Completed by frontend-dev with grouped renderer unit coverage and passing frontend build evidence |
| P1 | REB-10 | Implement all 21 TUT page templates and page routes | Task | DONE | frontend-dev | Yes: REB-07, REB-09 | 2026-07-08 local | Completed by frontend-dev with contract-driven template routing, template-wrapper tests, and passing frontend build evidence |
| P1 | REB-11 | Reimplement admin authoring/editor flows for new components/templates | Task | DONE | frontend-dev | Yes: REB-04, REB-08 | 2026-07-09 local | Completed by frontend-dev with passing admin Selenium round-trip coverage and full frontend build evidence under developer-owned verification |
| P1 | REB-12 | Implement Selenium public-site template/component suites | Task | DONE | devops | Yes: REB-06, REB-10 | 2026-07-09 local | Completed end-to-end: reseed + discovery fix + renderer/asset rewrites; REB-12 suite now `21 passing`, `0 pending`, `0 failing` |
| P1 | REB-13 | Implement Selenium admin authoring and round-trip suites | Task | DONE | devops | Yes: REB-06, REB-11 | 2026-07-10 local | Completed with passing admin round-trip Selenium suite + JUnit artifact refresh; proceed to REB-14 |
| P2 | REB-14 | Wire Selenium gates into CI/local validation and retain artifacts | Task | DONE | devops | No | 2026-07-11 local | Completed with passing Selenium smoke/full gates, retained artifacts, and critical/high traceability enforcement |
| P0 | REB-15 | QA verification for full rebuild program | Task | OPEN | qa | Yes: REB-14 | 2026-07-07 local | QA independently verifies complete rebuild evidence after delivery |
| P0 | REB-16 | PO acceptance for full rebuild program | Task | OPEN | po | Yes: REB-15 | 2026-07-07 local | PO accepts or rejects after QA pass |
| P0 | REB-17 | Prioritize authoring E2E automation backlog | Task | READY_FOR_PO | po | No | 2026-07-07 local | Cloud QA role completed; PO review is next. |
| P0 | REB-18 | Implement content tree, page creation, and author/publish verification Selenium suite | Task | DONE | devops | Yes: REB-11, REB-13 | 2026-08-23 local | Reconciled out of retired `READY_FOR_QA` per `DEC-DF-007`: evidence in `df/artifacts/REB-18/devops/summary.md`, suite green in the full gate (2 tests, 0 failures) |
| P0 | REB-19 | Implement page editor component/property/asset authoring matrix Selenium suite | Task | DONE | devops | No | 2026-08-19 22:35 CEDT | Completed: contract-driven authoring matrix (8 pass / 2 documented-blocker pending / 0 fail), CI gate wired, 495 backend tests + full frontend build + smoke/full Selenium gates green; REB-26 unblocked |
| P0 | REB-26 | Implement exhaustive per-UI-component sample-site editing Selenium suite | Task | DONE | devops | No | 2026-08-21 13:45 CEDT | Completed: 406/406 components PASS across ui/author-api/headless/graphql/publish (24 tests, 0 failures); 505 backend tests, frontend build 9/9, full Selenium gate PASS; four pre-existing blockers R26-1..R26-4 need SA routing |
| P0 | REB-20 | Implement publishing, workflow, scheduling, and bulk operation Selenium/API E2E suite | Task | DONE | devops | No | 2026-08-21 16:10 CEDT | Completed: 12 tests / 0 failures, 13 operation rows (8 PASS / 5 BLOCKED / 0 FAIL) across workflow, publish, bulk and scheduled operations with publish-environment verification; 505 backend tests, frontend build 9/9, full gate PASS (74 tests); five pre-existing blockers R20-1..R20-5 need SA routing |
| P1 | REB-21 | Implement DAM authoring and asset-reference Selenium E2E suite | Task | DONE | devops | No | 2026-08-23 11:10 CEDT | Completed: 10 tests / 0 failures, 9 operation rows; full gate PASS (84 tests); four blockers raised (R21-1..R21-4) of which R21-1/R21-2 have since been fixed and verified, R21-3/R21-4 need SA decisions |
| P1 | EDITOR-CANVAS-CLICK-INTERCEPT | Component links navigate away in edit mode instead of selecting the component | Bug | DONE | frontend-dev | No | 2026-08-24 local | Fixed with `pointer-events: none` on `.flexcms-canvas`: clicks fall through to the wrapper that already owns selection, so a click opens the component's properties while the anchor keeps its href and WYSIWYG fidelity is untouched. Placed on the canvas rather than in each renderer so it cannot be forgotten by the next renderer written. Links stay live in `/preview` and on the published site. Covered by `Editor WYSIWYG canvas suite S7`, which reproduced the reported navigation before the fix (clicked a CTA and landed on /tut-usa/vehicles) |
| P1 | EDITOR-PALETTE-DND | Cannot drag components from the left palette onto the page in the editor | Bug | DONE | frontend-dev | No | 2026-08-24 local | Root cause: the palette's draggables rendered outside the editor's `DndContext`, which only opened inside the canvas, so dnd-kit never started a drag. Context now wraps the whole editor body. Verified by real pointer drag (canvas 9 -> 10, dragged component present) and covered by `Editor WYSIWYG canvas suite S6`. The gate was green throughout because reordering existing components (`REB-19 S7`) is inside the context and passes, while palette insertion had no scenario |
| P2 | ADMIN-STABLE-SELECTORS | Add data-testid to headings on /sites, /dashboard, /dam and give /dam an h1 | Task | DONE | frontend-dev | No | 2026-08-24 local | `sites-heading`, `dashboard-heading` and `dam-heading` added; `/dam` gained an `<h1>Media Library</h1>`, named for the sidebar entry that leads there, so the route finally has an accessible page title. `REB-25 S8` now reports 10 PASS / 0 BLOCKED and no classified findings; `REB-24 S1` asserts the /dam heading instead of tolerating its absence; `/dam` is now included in the REB-25 accessibility smoke, having previously been the one route with a structural gap *and* the one route the check skipped |
| P1 | REB-22 | Implement experience-fragment and live-copy authoring Selenium E2E suite | Task | DONE | devops | No | 2026-08-23 local | 10 scenarios, 10 PASS / 0 BLOCKED; in the gate as `test:reb22:ci` (full gate 106 tests, 0 failures). Four defects found and fixed: XF publish replicated the variation without its components; live-copy missing source 500->404; duplicate target 500->409; malformed request body had no handler at all (500->400, every endpoint). Evidence: `df/artifacts/REB-22/devops/summary.md` |
| P1 | REB-23 | Implement PIM catalog/product authoring Selenium E2E suite | Task | DONE | devops | No | 2026-08-23 local | 10 scenarios, 10 passing; 11 matrix rows (10 PASS / 1 BLOCKED). In the gate as `test:reb23:ci` (full gate 116 tests, 0 failures). Seven PIM defects fixed — most importantly that a search-index failure blocked every product write, and that the version snapshot collided so no product could be updated at all. BLOCKED row is an Elasticsearch client/server version decision for the human. Evidence: `df/artifacts/REB-23/devops/summary.md` |
| P2 | REB-24 | Implement admin sites/translations/component-registry authoring-smoke Selenium suite | Task | DONE | devops | No | 2026-08-24 local | 8 scenarios, 8 passing; 8 matrix rows (6 PASS / 2 BLOCKED, both deliberate). In the gate as `test:reb24:ci` (full gate 124 tests, 0 failures). Found and fixed: `/components` rendered a blank document because the registry's 18 free-form group names were cast to a 7-member union and the colour lookup threw. Recorded as blockers: translations is static UI with no read endpoint, and the registry has no bridge into the editor. Evidence: `df/artifacts/REB-24/devops/summary.md` |
| P2 | REB-25 | Harden cross-cutting admin E2E quality gates for navigation, errors, accessibility, and cleanup | Task | DONE | devops | No | 2026-08-24 local | 10 scenarios, 10 passing; 10 matrix rows (9 PASS / 1 BLOCKED). Shared harness under `frontend/apps/selenium-e2e/src/harness/`: preflight, publish verifier that refuses an author URL, namespaced test data with a dependency-ordered cleanup audit, console/media health, a11y smoke, failure taxonomy. AC2 met via `AuthorApiClient`, so every existing publish read inherits the guard. In the gate as `test:reb25:ci` (full gate 134 tests, 0 failures). Evidence: `df/artifacts/REB-25/devops/summary.md` |

## Queue notes

- 2026-08-22 23:55 CEDT: devops completed `INFRA-TESTCONTAINERS-DOCKER29`. The `testcontainers-bom` 1.19.8 pin is gone — Testcontainers now comes from `spring-boot-starter-parent:4.1.0` (2.0.5), with the 2.x coordinate rename applied across `flexcms-core`/`flexcms-pim`/`flexcms-replication` and the four ITs moved off the deprecated `org.testcontainers.containers.*` aliases. `maven-failsafe-plugin` is bound to `integration-test` + `verify` with `**/*IT.java`, so **`mvn verify` now gates on integration tests** — `mvn test` never ran a single one (surefire's default includes do not match `*IT`), which is why these four suites had never executed even once. Evidence: `mvn clean verify` **BUILD SUCCESS, 552 tests, 0 failures** (505 unit + 47 IT: ContentNodeRepositoryIT 19, ProductRepositoryIT 13, ReplicationAgentIT 8, ReplicationReceiverIT 7) on Docker Engine **29.7.2** — the version that rejected the old line. AC2 proven by deliberately dropping the descendant clause from `ContentNodeRepository.deleteSubtree`: the build failed naming `deleteSubtree_removesNodeAndAllDescendants`, i.e. the very test that would have caught `BUG-CONTENT-DELETE`; the probe was reverted byte-for-byte and re-verified green. `CLAUDE.md` pre-push validation gained the IT stage, and its old "Backend unit + integration tests" label on `mvn test` — false, and part of why this went unnoticed — was corrected. **One product blocker for SA routing:** `I29-1` — `V4__tut_pim_sample_seed.sql` writes `products.status = 'ACTIVE'` but `ProductStatus` has no such constant, so all 4 seeded TUT products are unreadable through JPA on the live database; blocks `REB-23`. Recorded as `R-INFRA-002` in `risks.md`; `R-INFRA-001` is now closed. Four test-context gaps were fixed in-lane and documented in `blockers.md` (container extensions, over-broad test slices, missing core JPA + Jackson 2 ObjectMapper, untyped AMQP read).
- 2026-08-21 16:10 CEDT: devops completed `REB-20`. New suite `publishing-workflow-suite.spec.ts` covers the `standard-publish` workflow lifecycle (start / submit / approve / publish / reject / cancel), single-node and bulk publish, bulk move and delete, and both scheduled operations — **12 tests / 0 failures**, 13 operation rows (8 PASS / 5 BLOCKED / 0 FAIL). Publish verification is guarded: `S10` runs first and proves `:8081` is reachable *and* that an unpublished page is absent from it, so no later publish assertion can be satisfied by the author answering for itself (AC1). Scheduled scenarios pass deterministically against the real 60 s scheduler poll (observed 45-58 s), no clock manipulation (AC4). Evidence: `mvn test` 505/505, frontend build 9/9, Selenium full gate PASS (22+4+2+10+12+24 = 74 tests, 0 failures) with `test:reb20:ci` added to the gate and to critical traceability. Artifacts in `df/artifacts/REB-20/devops/`. **Five pre-existing blockers need SA routing, all `backend-dev`**: `R20-1` move leaves the moved node's parentPath stale so a page vanishes from its new parent and lingers in its old one (single **and** bulk move; the admin tree shows it in the folder it left); `R20-2` a page that ever carried a workflow is permanently undeletable (workflow_instances FK, opaque HTTP 500) — needs `sa` on intended semantics; `R20-3` scheduled publish never transitions the author status, so the public is served a page the author shows as DRAFT; `R20-4` scheduled deactivation retracts nothing, confirming REB-26 `R26-2` via a second path; `R20-5` bulk delete counts a never-existing path as succeeded, unlike bulk publish/move. Recorded as `R-REB-20-001..005` in `risks.md`. One infrastructure rerun: the first gate attempt hit `session not created from chrome not reachable` in REB-26's before-all hook; a driver probe then started a session in 926 ms and the retry passed.
- 2026-08-21 13:45 CEDT: devops completed `REB-26`. The exhaustive sweep now covers **all 406 active component contracts with 406 `PASS`, 0 FAIL / 0 BLOCKED / 0 UNSUPPORTED_UI / 0 SKIPPED** — `ui`, `author-api`, `headless`, `graphql` and `publish` proven for every component, 2172 field-level rows, and every contracted editor control rendered (0 missing). Evidence: `pnpm test:reb26:ci` and the full gate's REB-26 stage both 24 tests / 0 failures with identical matrices; `mvn test` 505/505; frontend build 9/9; Selenium full gate PASS (22+4+2+10+24 tests, 0 failures). Artifacts in `df/artifacts/REB-26/devops/` (`summary.md`, `component-editing-matrix.csv`, `field-coverage.csv`, `test-scenarios.md`, `blockers.md`). **Four pre-existing blockers need SA routing**: `R26-1`/`R26-2` (delete and unpublish never reach the publish environment — `backend-dev` + `sa`), `R26-3` (numeric fields cannot be cleared; clear-then-retype corrupts the value — `frontend-dev`), `R26-4` (reference site renders interactive components as read-only previews; 42 of 42 form components render no interactive control — `frontend-dev` + `sa`). Two environment faults were diagnosed, not product bugs: backends started without the `local` profile answer `401` to every author write (first sweep attempt discarded), and Next.js **dev** servers serve HTML for `/_next/static/chunks/*.js`, which fails REB-12's console-error assertion — the gate must run against production frontend builds. Both recorded in `hints_for_agent.md`.
- 2026-08-19 23:05 CEDT: backend-dev completed `BUG-CONTENT-DELETE` and `BUG-PUBLISH-REPLICATION`. Backend suite is now **505 tests / 0 failures** (was 495; +10 new), Selenium full gate PASS (48 tests / 0 failures), and both fixes were verified live against the running stack. REB-19 S10 no longer records the publish blocker and its matrix dropped from 79 to 78 rows because the BLOCKED row disappeared; the suite also cleans up its own fixture page again. New `INFRA-TESTCONTAINERS-DOCKER29` raised: Testcontainers 1.19.8 cannot talk to Docker Engine 29, and no build stage runs the `*IT` tests at all — a pre-existing `deleteSubtree` IT would otherwise have caught `BUG-CONTENT-DELETE` long ago.
- 2026-08-19 22:52 CEDT: `sa` hat (Mode B, brief) raised two P0 bugs from REB-19 evidence and routed both to `backend-dev`: `BUG-CONTENT-DELETE` (REB-19 blocker B-7) and `BUG-PUBLISH-REPLICATION` (REB-19 blocker B-5). Bugs precede enhancements at equal priority, and `BUG-CONTENT-DELETE` blocks `REB-26` from cleaning up the fixture pages it will create for 406 components. Task definitions in `df/artifacts/{BUG-CONTENT-DELETE,BUG-PUBLISH-REPLICATION}/task.md`.
- 2026-08-19 22:35 CEDT: devops completed `REB-19`. The suite is fully contract-driven — every component, field, control type, and test id derives from `component-contracts.json`, so `REB-26` reuses it without new field-type knowledge. Evidence: 8 passing / 2 pending / 0 failing, `mvn test` 495/495, frontend build 9/9, Selenium smoke and full gates PASS (22+4+2+10 tests, 0 failures). Seven pre-existing implementation blockers are recorded in `df/artifacts/REB-19/devops/blockers.md`; **B-5 needs SA routing to `backend-dev`** — publishing from the page editor calls `/node/status`, which never replicates, so an edit published from the UI never reaches the publish environment. Two repository defects were fixed on the way: `src/reports/hooks.ts` had never been committed (unanchored `reports/` gitignore pattern blocked every Selenium suite on a fresh clone) and `selenium-gate.cjs` could not spawn pnpm on Windows. New `scripts/publish_tut_usa_site.py` publishes a freshly seeded database to the publish environment (72/72 pages verified).
- 2026-08-24 local: completed `ADMIN-STABLE-SELECTORS`. Worth recording a check that came out the opposite way to my suspicion: after the fix, REB-25 reported "no classified findings" where it had previously reported one, which looked like the suite quietly stopping finding things — the exact failure mode its own ignore-list assertions guard against. Probing the accessibility helper directly across all five routes returned zero unlabelled controls, so the single earlier finding was `S8`'s missing-selector one, resolved by this task, and `S7` had never had naming gaps. No regression; the report is correct.
- 2026-08-24 local: fixed `EDITOR-CANVAS-CLICK-INTERCEPT`, a regression this session introduced. Making the canvas render the site's real renderers also gave it real anchors, so clicking a CTA followed the link and left the editor with unsaved work. `S7` was written first and failed against the unfixed build with the exact reported symptom — clicking a CTA navigated to `/tut-usa/vehicles` — which is what shows the scenario discriminates rather than passing vacuously. One rule on `.flexcms-canvas` fixes it; the WYSIWYG suite is 7/7.
- 2026-08-24 local: fixed `EDITOR-PALETTE-DND`. The palette's `DraggablePaletteItem` calls `useDraggable`, but `DndContext` only opened inside the canvas `<section>` — so every palette tile was outside the context and dnd-kit never started a drag. Reordering components already on the canvas kept working, because those live inside the context, and that passing behaviour (`REB-19 S7`) is why the gate never showed a problem. The context now wraps the whole editor body. Verified with a real pointer drag: canvas went 9 -> 10 components and the dragged promo-banner is present. Covered by `Editor WYSIWYG canvas suite S6`, which drags with a pointer sequence rather than clicking, because the editor's click-to-insert path worked throughout the outage and a click-based assertion would have proved nothing.
- 2026-08-24 local: the human reported that links inside components navigate away during editing. Registered as `EDITOR-CANVAS-CLICK-INTERCEPT`, and recorded as a regression introduced by the WYSIWYG canvas change rather than a pre-existing defect: real renderers emit real anchors, which the grey placeholders they replaced did not.
- 2026-08-24 local: the human reported that Experience Fragment authoring was still broken, with a screenshot of `/editor?path=%2Fcontent%2Fexperience-fragments%2Ftut-usa%2Fglobal%2Fnavigation` showing "Renderer pending: flexcms/xf-page". The URL is the fragment **folder**: the editor loaded it and rendered its `master` variation as though it were a component, so nothing was editable. The earlier fix had only corrected the two hardcoded links in the page editor, and the content tree's row action menu builds its Edit href from the node's `urlPath`, so the folder URL is reachable by browsing — and thereafter by bookmark.
- 2026-08-24 local: root cause of the E2E miss, and why 100% authoring coverage did not catch it: both existing scenarios opened the editor on a path they constructed themselves (`${xfPath}.master` in REB-22 S4, a literal `.../navigation/master` in the WYSIWYG suite). They verified the destination and never the navigation, so the application was free to build any wrong URL and the suites still passed. Fixed by resolving a fragment folder to an editable variation inside the editor — which covers every entry point, not just the links that were known — and by adding REB-22 `S11`-`S15`: the reported folder URL, the content tree's own Edit link, the editor's own fragment link, an invariant that no structural node may ever be rendered as a component whatever the URL, and a guard that Save writes to the node on screen rather than the node named in the URL.
- 2026-08-24 local: while fixing the above, found that the resolution would have corrupted fragments on its own: `handleSave` and the publish handler both derived their ltree path from the URL, so with the editor showing a variation they would have parented new components to the folder and published the folder. Both now use the loaded node's path.
- 2026-08-24 local: devops completed `REB-25` (`DONE`), which clears the last open REB task. The deliverable is a shared harness rather than another product suite: environment preflight that names the unreachable endpoint and the variable pointing at it; a `PublishVerifier` that refuses to construct when the publish URL resolves to the author host — the failure it prevents is a publish assertion that passes for every page because the author instance serves unpublished content; a test-data namespace whose cleanup registers each deleter at creation and tears down newest-first, which is precisely what REB-23's teardown could not do when it stranded ten catalogs; console health whose ignore list is asserted not to match a real TypeError, since two defects in this program were visible only in the console; accessibility smoke; and a four-class failure taxonomy. AC2 was met by deriving `AuthorApiClient`'s publish base from the verifier, so REB-18/19/20/21/23/26 inherit the guard without edits to their call sites. Recorded as BLOCKED: `/sites`, `/dashboard` and `/dam` have no stable heading selector, and `/dam` renders no `h1` at all.
- 2026-08-24 local: devops completed `REB-24` (`DONE`). Suite `secondary-routes-suite.spec.ts` covers all eight required scenarios; the full gate passed at 124 tests across 13 suites. The route the task centres on was broken: `/components` rendered an empty document — no heading, no navigation, zero focusable elements — because `apiToComponentDef` cast the registry's group name to a seven-member union, so the colour lookup was `undefined` for every component and reading `.bg` threw during render. The `as` cast is why this never failed a type-check. Two surfaces are recorded as blockers rather than fixed: the translations matrix issues no request and no read endpoint exists to call (the i18n model and service exist, but nothing exposes the dictionary), and the component registry offers no navigation into the page editor. Also observed: `/dam` renders no `h1`, unlike every other admin route.
- 2026-08-24 local: REB-24 closing leaves `REB-25` with every dependency `DONE` — it is now the only remaining board task.
- 2026-08-24 local: devops started `REB-24` (`DEV_IN_PROGRESS`). REB-24 is the last dependency blocking `REB-25`, the cross-cutting hardening task. Initial reading of the four routes: `translations` performs no fetch at all, so it is static UI rather than backed authoring — the distinction AC1 asks to be evidenced — and the component registry offers no navigation into the page editor, which scenario 5 expects to be recorded as a blocker.
- 2026-08-23 local: devops completed `REB-23` (`DONE`). Suite `pim-authoring-suite.spec.ts` covers all ten required scenarios and is registered in the gate's full mode; the full gate passed at 116 tests across 12 suites. The PIM needed substantial repair before the scenarios could run at all: indexing ran inline and rethrew, so an unreachable Elasticsearch made every product *write* answer 500; the version snapshot read `Product.version` before the `@PreUpdate` increment had flushed, so no product could be updated; and lazy proxies broke the variant, asset-ref, status, merge and catalog-listing responses. Also fixed: the admin catalog list read a paged envelope as an array and rendered "No catalogs found" over the failure. Left for a human decision: Boot 4.1 manages an Elasticsearch 9.x client while docker-compose pins the server to 8.13.4, so product search is unavailable — the suite records it as its only BLOCKED row and asserts authoring survives the outage.
- 2026-08-23 local: devops started `REB-23` (`DEV_IN_PROGRESS`). PIM seeding was repaired earlier in this session under the blocker sweep (`I29-1`: `products.status = 'ACTIVE'` is not a value `ProductStatus` can parse, so every seeded product was unreadable), which is what makes this task workable now.
- 2026-08-23 local: devops completed `REB-22` (`DONE`). Suite `reusable-content-suite.spec.ts` covers all ten required scenarios, 10 PASS / 0 BLOCKED, and is registered in the gate's full mode; the full gate passed at 106 tests across 11 suites. Behaviour was probed against the running system before any assertion was written, which is what surfaced the four defects fixed under this task — most importantly that publishing an Experience Fragment variation replicated the variation but not its components, so reusable content reached the publish instance as an empty shell while the equivalent page operation worked. Two existing `LiveCopyServiceTest` cases were found to be pinning the 500-producing exception types and were corrected to assert the 404/409 contract. Left as a recorded observation rather than changed: rollout of a nonexistent source answers 200 with `updatedNodes=0`, so a mistyped blueprint path is indistinguishable from one with no live copies.
- 2026-08-23 local: devops started `REB-22` (`DEV_IN_PROGRESS`). Immediately before this, the human reported that Experience Fragment editing did not work; six defects were found and fixed across the admin page and `flexcms-author` (see `df/artifacts/EXPERIENCE-FRAGMENTS-2026-08-23/summary.md`), including `XF_ROOT` sitting outside `content.` — which made any fragment created through the API unreachable by that same API — and `Instant.now()` bound as a JDBC parameter, which made every variation change answer HTTP 500. REB-22 covers exactly these flows, so the suite is being written against a backend that can now support them.
- 2026-08-23 local: devops reconciled `REB-18` from the retired `READY_FOR_QA`/`qa` state to `DONE` under `DEC-DF-007`. Its evidence and a green suite in the full gate already meet the developer bar that replaced QA, and it is a declared REB-22 dependency.
- 2026-08-19 21:29 CEDT: devops started `REB-19` (`DEV_IN_PROGRESS`) in Mode B (interactive; router not started). Dependency flag corrected to `No` because REB-07/REB-10/REB-11/REB-13 are all `DONE`. Session opened on a fresh workstation with **no toolchain**: no Maven, no pnpm, no `~/.m2`, empty Docker, no `node_modules`/`target`. Provisioning was authorised by the human before delivery work began.
- 2026-07-12 local: frontend-dev completed `TUT-LINK-RENDERING`. Clean Next.js restart resolved the generated vendor-chunk runtime failure; authored-link integrity passed, REB-12 template coverage passed `22 tests / 0 failures`, Selenium smoke/full gates passed, and retained artifacts were generated under `frontend/apps/selenium-e2e/reports/retained/{smoke,full}`.
- 2026-07-11 21:21 CEST: backend-dev completed `TUT-LINK-CONTRACTS`. Docker Hub access recovered; the build then exposed pre-existing Java 26/Spring Boot 4 Docker drift, corrected by using Temurin 26 builder/runtime images and Boot 4 `tools` layered extraction. Exact Docker build PASS (37/37), image runtime/package validation PASS, all prior 495-test/Maven/PostgreSQL evidence remains green, and `TUT-LINK-SEED` is now unblocked for data-engineer.
- 2026-07-11 18:20 CEST: backend-dev completed `TUT-LINK-CONTRACTS` implementation and verification except the mandatory Docker image gate: focused contract tests 2/2 PASS, full backend 495/495 PASS, clean compile/install PASS, and PostgreSQL 16 V18 execution PASS. Three Docker attempts failed/timed out resolving uncached Docker Hub frontend/base images, so the task is correctly `BLOCKED` and `TUT-LINK-SEED` remains dependency-blocked.
- 2026-07-11 17:56 CEST: backend-dev started `TUT-LINK-CONTRACTS`; confirmed V18 is the next CMS migration and scoped surgical schema corrections to navigation, footer, and featured-content definitions with migration plus public-registry contract coverage.
- 2026-07-11 17:24 CEST: SA refined the user-reported seeded-site broken-link defect as `TUT-LINK-INTEGRITY`, confirmed root causes across registry schemas, `scripts/seed_tut_usa_website.py`, `homepageRenderers.tsx`, and Selenium coverage, then split delivery into `TUT-LINK-CONTRACTS` -> `TUT-LINK-SEED` -> `TUT-LINK-RENDERING`. The frontend child also waits for `BUG-TUT-VEHICLE-RENDERER` because both may touch shared renderer registration files.
- 2026-07-11 local: DevOps completed REB-14 by adding Selenium smoke/full gate commands, retained-artifact bundling (`reports/retained/{smoke,full}`), and critical/high traceability enforcement via `frontend/apps/selenium-e2e/scripts/selenium-gate.cjs`; validation PASS for `pnpm --filter @flexcms/selenium-e2e ci:gate:smoke` and `pnpm --filter @flexcms/selenium-e2e ci:gate:full`.
- 2026-07-10 local: DevOps completed REB-13 evidence refresh by starting full local stack, rerunning `pnpm test:admin` to `4 passing / 0 failing`, regenerating REB-13 JUnit (`reb13-admin-suite.xml`), and validating full frontend `pnpm build` PASS. Targeted backend `ContentNodeServiceTest` remains blocked in this shell by Java 26 + Mockito/ByteBuddy incompatibility (known environment constraint).
- 2026-07-09 local: End-to-end fix applied in one continuous session across data/backend/frontend/devops concerns for REB-12: reseeded TUT-USA pages, switched page discovery to recursive tree traversal, forced dynamic site rendering, added missing DAM rewrite fallback, fixed grouped renderer image-source validation, and mapped `TPL-03`. Final evidence: `pnpm test:templates` => `21 passing`, `0 pending`, `0 failing`.
- 2026-07-09 local: Frontend-dev addressed REB-12 render-path issues by forcing dynamic SSR in `site-nextjs` and running browser checks against `SITE_URL`; REB-12 rerun is now `4 passing / 17 pending / 0 failing` with refreshed JUnit/status artifacts.
- 2026-07-09 local: DevOps restarted local author/publish/site, bulk-published `/content/tut-usa` (200/succeeded=1), and reran REB-12. Result remains `1 passing / 14 pending / 6 failing`; failures now consistently report main-content missing (header/footer-only risk) on TPL-01/06/08/09/10/11.
- 2026-07-09 local: Frontend-dev completed `REB-11` under `DEC-DF-007` by validating admin authoring/editor flows with Selenium (`ADMIN_URL=http://localhost:3100`) plus full frontend build PASS; task moved to `DONE`.
- 2026-07-09 local: Human requested "take task" and selected `REB-11`. To align with `DEC-DF-007` (QA/PO retired), task ownership moved from retired `READY_FOR_QA` to active `DEV_IN_PROGRESS` in the `frontend-dev` lane.
- 2026-07-07 local: Human requested a completely new backlog replacing the previous RT/TF queue. Previous board archived at `df/artifacts/REB-00/archived-board-before-reset.md` before replacement.
- Active rebuild source artifact: `df/artifacts/REB-00/solution-design.md`.
- Child task artifacts exist under `df/artifacts/REB-01/` through `df/artifacts/REB-16/` with dependencies and acceptance criteria.
- Existing Playwright tests are retained until Selenium replacement coverage is implemented, QA-passed, and PO-accepted.
- 2026-07-07 local: PO accepted REB-00 (`df/artifacts/REB-00/po-review.md`). REB-01 is now unblocked; all other tasks remain blocked per their documented dependency chain.
- 2026-07-08 local: Human decision `DEC-DF-007` — `qa` and `po` roles are **permanently disabled in all operating modes**, superseding `DEC-REB-005`/`DEC-REB-006`/`DEC-DFCA-002`. The states `READY_FOR_QA`/`QA_IN_PROGRESS`/`QA_FAILED`/`READY_FOR_PO`/`PO_REVIEW`/`PO_REJECTED` are retired; the delivery lane now carries work directly `DEV_IN_PROGRESS -> DONE` after meeting the developer testing bar (100% unit/Selenium coverage designed, run, and green + zero-error full build). Tasks still parked in the retired states and the QA/PO-only tasks (REB-15, REB-16) need reconciliation — see next action.
- 2026-07-07 local: Human clarified TUT design is already provided and no designer-side action is needed for generating REB-04 contracts/page-tree. REB-04 proceeded in backend-dev lane and is ready for QA.
- 2026-07-07 local: Human decision `DEC-REB-005` — `qa` and `po` automated role sessions are temporarily disabled; the human will play both roles manually. See `df/03-orchestration-rules.md` (active override section) and `df/runtime/decisions.md`. Tasks in `READY_FOR_QA`/`QA_IN_PROGRESS`/`READY_FOR_PO`/`PO_REVIEW` now wait for human action instead of an automated role session.
- 2026-07-07 local: Human accepted `DEC-REB-006` to re-enable automated `qa` and `po` role sessions. `DEC-REB-005` is now superseded for new routing decisions; tasks in QA/PO states are actionable by the corresponding role again.
- 2026-07-07 local: Corrected stale `Blocked?` flag on REB-05 — its only dependency, REB-00, is `DONE`, so REB-05 is unblocked. Implemented the Selenium framework foundation (`frontend/apps/selenium-e2e`) end-to-end: install, `tsc` build, and a passing `@smoke` spec with JUnit-report evidence recorded in `df/artifacts/REB-05/devops/summary.md`. REB-05 is now `READY_FOR_QA` and awaits manual human review per `DEC-REB-005`.
- 2026-07-07 local: Designer completed REB-01 design normalization artifacts (`Design/tut-usa/README.md`, `df/artifacts/REB-01/design/inventory.md`, `df/artifacts/REB-01/design/summary.md`) and handed off storage-map/inventory scope to DevOps (`df/artifacts/REB-01/handoffs.md`).
- 2026-07-07 local: DevOps reviewed REB-01 storage-map/inventory artifacts, recorded approval evidence in `df/artifacts/REB-01/devops/summary.md`, and moved REB-01 to `READY_FOR_QA` for manual human review per `DEC-REB-005`.
- 2026-07-07 local: DevOps implemented REB-02 in `frontend/apps/selenium-e2e`, ran the browser-first capture pipeline, and generated `Design/tut-usa/manifest.json`, per-page manifests, normalized HTML, screenshots, and captured assets. REB-02 is now `READY_FOR_QA` for manual human review per `DEC-REB-005`.
- 2026-07-07 local: Data-engineer implemented `scripts/reset_tut_usa_seed.py` plus unit coverage in `scripts/tests/test_reset_tut_usa_seed.py`, generated `df/artifacts/REB-03/data/reset-scope.json`, and documented the reset/reseed plan in `df/artifacts/REB-03/data/`. REB-03 is now `READY_FOR_QA` for manual human review per `DEC-REB-005`. Live DB row counts were designed into the tool but not captured in this shell because `psycopg2` is not installed.
- 2026-07-07 local: Human requested starting a new dev task while QA/PO remain disabled and manual review comes later. REB-06 was explicitly started (`DEV_IN_PROGRESS`) using REB-02/04/05 outputs already produced in `READY_FOR_QA`; human acceptance of those upstream tasks will happen later per `DEC-REB-005`.
- 2026-07-07 local: DevOps completed REB-06 by generating `Design/tut-usa/generated/qa-traceability-matrix.csv`, fixture manifests, and Selenium skeleton specs for 21 templates + 14 component groups (including skipped `tut_sovereign` rows marked `skeleton-no-capture`). Task moved to `READY_FOR_QA` for manual human review per `DEC-REB-005`.
- 2026-07-07 local: Human requested starting another new dev task while QA/PO remain disabled and manual review comes later. REB-07 was explicitly started (`DEV_IN_PROGRESS`) using produced outputs from REB-02 and REB-03 in `READY_FOR_QA`; manual acceptance of upstream tasks remains deferred per `DEC-REB-005`.
- 2026-07-07 local: Data-engineer completed REB-07 by implementing `scripts/import_tut_usa_captured_assets.py`, copying 182 captured assets into site/admin public roots, and generating `df/artifacts/REB-07/data/dam-import-map.json` plus checksum/rollback/validation evidence. Task moved to `READY_FOR_QA` for manual human review per `DEC-REB-005`.
- 2026-07-07 local: Human requested starting the next implementation task. REB-08 was explicitly started (`DEV_IN_PROGRESS`) in the `frontend-dev` lane while QA/PO remain manual-only per `DEC-REB-005`; implementation uses approved design artifacts from REB-01 and generated contracts from REB-04 pending manual acceptance.
- 2026-07-07 local: Human requested starting the next implementation task. REB-09 was explicitly started (`DEV_IN_PROGRESS`) in the `frontend-dev` lane while QA/PO remain manual-only per `DEC-REB-005`; implementation used REB-08 renderer foundation and generated contracts from REB-04 pending manual acceptance.
- 2026-07-07 local: Frontend-dev completed REB-08 by wiring captured TUT fonts/tokens into site shell, replacing stale renderer imports with a contract-aware fallback map, updating React renderer metadata props, and validating `cd frontend && NUXT_TELEMETRY_DISABLED=1 pnpm build` PASS. Task moved to `READY_FOR_QA` for manual human review per `DEC-REB-005`.
- 2026-07-07 local: Frontend-dev completed REB-09 by adding grouped TUT renderer implementations and contract-driven registration across all generated `tut-usa/*` resource types in `site-nextjs`; validated with `cd frontend && NUXT_TELEMETRY_DISABLED=1 pnpm build` PASS. Task moved to `READY_FOR_QA` for manual human review per `DEC-REB-005`.
- 2026-07-07 local: DevOps started REB-13 (`DEV_IN_PROGRESS`) by implementing initial Selenium admin round-trip coverage under `frontend/apps/selenium-e2e/src/cases/admin/authoring-roundtrip.spec.ts` with supporting page/API helpers and command `pnpm test:admin`.
- 2026-07-07 local: DevOps started REB-12 (`DEV_IN_PROGRESS`) by adding a public-site home+rest pages suite `frontend/apps/selenium-e2e/src/cases/templates/tut-usa-pages.spec.ts` with dynamic seeded-page discovery, route render checks, and image-health assertions; validated with `pnpm test:pages` PASS.
- 2026-07-07 local: SA added prioritized authoring E2E automation backlog `REB-17` through `REB-25`. Coverage maps current authoring surfaces and requires page creation tests to verify create + publish + authoring availability, and publishing tests to verify publish-environment visibility.
- 2026-07-07 local: Human requested E2E editing coverage for each sample-site UI component. SA added `REB-26` requiring one generated editing scenario or explicit blocker row for every active component in `Design/tut-usa/generated/component-contracts.json` (currently 406 components across 14 groups).
- 2026-07-07 local: Human requested changing Dark Factory from local GitHub Copilot CLI execution to GitHub Copilot Cloud Agent tasks launched through the GitHub REST API so premium model choices can be used. SA created `DFCA-01`, recorded `DEC-DFCA-001`, and routed implementation to `devops`.
- 2026-07-07 local: Human instructed this agent not to play QA and requested disabling/removing PO. Recorded `DEC-DFCA-002`; automated `po` routing is disabled until further notice, while QA remains a separate non-current role/session.
- 2026-07-08 local: Human requested starting a new implementation task. Frontend-dev started `REB-11` (`DEV_IN_PROGRESS`) and began editor modernization for UI-kit controls, breadcrumb/skeleton/empty states, and stable Selenium selectors in `frontend/apps/admin/src/app/editor/page.tsx`.
- 2026-07-08 local: Human requested starting a new dev task. DevOps started `REB-18` (`DEV_IN_PROGRESS`) under explicit dependency override while `REB-11`/`REB-13` remain in progress; this session begins with REB-18 environment readiness + baseline suite scaffolding.
- 2026-07-08 local: Human explicitly verified and accepted `REB-02` through `REB-08`; board states updated to `DONE`.
- 2026-07-08 local: Human requested starting a new task. To align with permanent QA/PO retirement (`DEC-DF-007`), `REB-09` was pulled from retired `READY_FOR_QA` into `DEV_IN_PROGRESS` in the `frontend-dev` lane for developer-owned completion.
- 2026-07-08 local: Frontend-dev completed `REB-09` under the developer testing bar by adding `site-nextjs` grouped-renderer unit coverage (`vitest`) and recording PASS evidence for `pnpm --filter @flexcms/site-nextjs test` plus full frontend `pnpm build`.
- 2026-07-08 local: Frontend-dev completed `REB-10` under the developer testing bar by switching template routing to generated contract names, adding template-map/wrapper regression tests, and validating PASS for `pnpm --filter @flexcms/site-nextjs test` and full frontend `pnpm build`.
- 2026-07-08 local: DevOps reran `REB-12` template suites after `REB-10`; status regressed to `2 passing / 18 pending / 1 failing` with hard failure `TPL-08` (`/tut-usa` console errors) plus unresolved seed/image blockers. REB-12 remains `DEV_IN_PROGRESS`.
- 2026-07-08 local: DevOps fixed REB-12 false positives by running template checks against publish URL and tightening CTA/main-content/responsive assertions. Current status is stricter: `1 passing / 14 pending / 6 failing` on runnable publish routes, which now exposes header/footer-only style outcomes as failures.
- 2026-07-08 local: DevOps added author-vs-publish parity diagnostics to REB-12. Failures now clearly separate publish endpoint errors (`500`) from publish content loss (`author has components, publish has 0`), including `/tut-usa/home` mismatch evidence.
- 2026-07-08 local: Author bulk publish now uses tree replication for page/site-root nodes (`AuthorContentController.bulkPublish`) to address publish component-loss parity. Source compiles; live verification pending runtime restart and rerun.
