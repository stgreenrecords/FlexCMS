{
  "name": "@flexcms/selenium-e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "capture:tut-assets": "pnpm build && node dist/src/capture/runTutAssetCapture.js",
    "capture:tut-assets:headed": "cross-env HEADLESS=false pnpm capture:tut-assets",
    "generate:traceability": "ts-node src/capture/generateTraceabilitySkeletons.ts",
    "test": "pnpm build && mocha",
    "test:smoke": "pnpm build && mocha --grep @smoke",
    "test:smoke:ci": "cross-env CI=true pnpm build && cross-env CI=true mocha --grep @smoke --reporter mocha-junit-reporter --reporter-options mochaFile=./reports/junit/selenium-smoke-results.xml",
    "test:admin": "pnpm build && mocha --grep \"REB-13 admin authoring and round-trip suite\"",
    "test:admin:ci": "cross-env CI=true pnpm build && cross-env CI=true mocha --grep \"REB-13 admin authoring and round-trip suite\" --reporter mocha-junit-reporter --reporter-options mochaFile=./reports/junit/reb13-admin-suite.xml",
    "test:reb18": "pnpm build && mocha --grep \"REB-18 content tree and page lifecycle suite\"",
    "test:reb18:ci": "cross-env CI=true pnpm build && cross-env CI=true mocha --grep \"REB-18 content tree and page lifecycle suite\" --reporter mocha-junit-reporter --reporter-options mochaFile=./reports/junit/reb18-suite.xml",
    "test:pages": "pnpm build && mocha --grep \"REB-12 public site pages\"",
    "test:templates": "pnpm build && mocha --grep \"REB-12 template-by-template public site coverage\"",
    "test:templates:ci": "cross-env CI=true pnpm build && cross-env CI=true mocha --grep \"REB-12 template-by-template public site coverage\" --reporter mocha-junit-reporter --reporter-options mochaFile=./reports/junit/reb12-template-results.xml",
    "test:headed": "cross-env HEADLESS=false pnpm build && cross-env HEADLESS=false mocha",
    "test:debug": "cross-env HEADLESS=false SLOWMO=250 pnpm build && cross-env HEADLESS=false SLOWMO=250 mocha --grep @smoke",
    "test:ci": "cross-env CI=true pnpm build && cross-env CI=true mocha --reporter mocha-junit-reporter --reporter-options mochaFile=./reports/junit/selenium-results.xml",
    "test:full:ci": "pnpm test:templates:ci && pnpm test:admin:ci && pnpm test:reb18:ci",
    "ci:gate:smoke": "node scripts/selenium-gate.cjs --mode smoke",
    "ci:gate:full": "node scripts/selenium-gate.cjs --mode full",
    "clean": "rimraf dist reports test-results"
  },
  "devDependencies": {
    "@types/chai": "^4.3.16",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "@types/selenium-webdriver": "^4.1.21",
    "chai": "^4.4.1",
    "chromedriver": "^124.0.0",
    "cross-env": "^7.0.3",
    "mocha": "^10.4.0",
    "mocha-junit-reporter": "^2.2.1",
    "rimraf": "^5.0.5",
    "selenium-webdriver": "^4.20.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.4.5"
  }
}


